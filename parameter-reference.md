# ShuttleScope 参数体系 — 完整参考文档

## 原始数据层

每一场比赛的原始记录包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `sc` | `[[a,b], ...]` | 每局比分，如 [[14,21], [21,19], [25,23]] |
| `pts` | `[[[a,b], ...], ...]` | 逐分累计比分，每局为一个数组（第一元素= hid 方得分，第二= aid 方得分） |
| `dt` | `[[s, ...], ...]` | 逐分点间间隔（秒），每局长度的 1 比 pts 各少 1 |
| `dur` | `float` | 比赛总时长（分钟） |
| `hid` / `aid` | `int` | 主/客球员 ID |
| `w` | `'h'/'a'` | 胜方 |
| `tn` | `string` | 赛事名称 |
| `date` | `YYYY-MM-DD` | 日期 |

---

## 一、维度卡片—参数总览

以下按网站 5 卡分类。

---

### 维度 1: 压制力（连续得分能力）

测度球员在短时间内连续得分、统治比赛节奏的能力。

#### 1a. 得分跑序列 (Streaks)
从 `pts` 序列中提取**连续得分**的长度。

- **计算**：对每一局，逐分检查"谁得这一分"。若球员 A 连续得 N 分，则构成一个长度为 N 的 streak。
- **输出**：每局得到一个 streak 数组。如 Set 3 石宇奇: `[2,1,1,2,2,7,2,2,1,2,1,2]`
- **单位**：次（事件数）

#### 1b. 3+/4+/5+ 连得频率
特定长度 streak 出现的**事件次数**（不是总分）。

- **计算**：对每局，统计 streak 长度 ≥3、≥4、≥5 的出现次数。注意：一个长度为 7 的 streak 只计 1 次"3+连得事件"（不是 5 次，因为它是 1 个连续事件）
- **输出**：`streak3pFreq` / `streak4pFreq` / `streak5pFreq`
- **单位**：次
- **展示**：**次/局**（除以局数后展示）

#### 1c. 3+连得出现率
出现至少一次 3+连得的局占总局数的比例。

- **计算**：`streak3pSets / totalSets * 100`
- **展示**：百分比

#### 1d. 最大连得
所有局中最大 streak 长度。

- **输出**：`maxStreak`
- **单位**：分

---

### 维度 2: 韧性与稳定性

测度球员在逆境中的韧性（落后能否翻盘）和领先时的稳定性（领先能否守住）。

#### 2a. 翻盘率 — 按落后分差 (Comeback Rate)
当球员在某局中**落后 N 分及以上**后，该局最终**获胜**的比率。

- **计算**：
  1. 对每局遍历 `pts` 数组，计算 hid 与 aid 的分差 `d`
  2. 若 `d < 0`（球员落后），记录该局最小分差 `minDiff`
  3. 若 `minDiff ≤ -N`，则该局计入落后 N 分的"尝试次数"`cb[N].d`
  4. 若该局最终获胜，计入"翻盘成功次数"`cb[N].w`
- **输出**：`cb[4/5/6/7].d` 和 `cb[4/5/6/7].w`
- **展示**：`w / d * 100 %` 加 `(w/d)` 样本
- **注意**：N 分差是指"最大落后幅度≥N"，不是"正好落后 N 分"

#### 2b. 落后翻盘率（全局）
全局维度——球员在**所有落后局**中的翻盘占比。

- **计算**：`trailWon / (totalSets - neverTrailed)`
  - `neverTrailed`：该局从未落后的局数
  - `trailWon`：落后但最终获胜的局数
- **展示**：百分比

#### 2c. 最大翻盘
球员最大落后分差中的最大者（且最终翻盘成功）。

- **输出**：`maxComeback`
- **单位**：分

#### 2d. 领先稳定性
当球员在某局中领先达到 N 分时，该局**输掉**的比率。

- **计算**：
  1. 对每局遍历 `pts`，记录最大领先分差 `maxLead`
  2. 若 `maxLead ≥ N`，则该局计入"领先 N 分的局数"`leadNSetTotal`
  3. 若该局最终**输掉**，计入"领先 N 分被翻盘的局数"`leadNSetLoss`
- **输出**：`lead3SetLoss/Total`, `lead5SetLoss/Total`, `lead7SetLoss/Total`
- **展示**：百分比 + `(总样品局数)`

#### 2e. 领先交替次数
**每局**中领先权在两人之间交替的平均次数。

- **计算**：
  1. 对每局遍历 `pts` 序列，追踪分差 `d` 的正负
  2. 每次 `d` 从正→负或负→正，计 1 次"领先交替"
  3. 统计所有局的交替总数 ÷ 有交替的局数
- **展示**：`总交替次数 / 有交替的局数` → "次/局"

#### 2f. 从未落后率
该球员**从未在对手领先的情况下赢下一局**的比例。

- **计算**：`neverTrailed / totalSets * 100`
- **展示**：百分比

#### 2g. 止损效率 (Momentum Conversion)
连失 2 分后，平均还要继续丢多少球才能**重新得分**。

- **计算**：
  1. 遍历 `pts` 序列，跟踪"连续丢分"长度 `lossSeq`
  2. 当选手终于得分时（`lossSeq ≥ 2`），记录此次连失的长度
  3. 对所有这样的序列求和 ÷ 次数
- **输出**：`mcSum / mcTotal` → "平均恢复所需丢分数"
- **展示**：`X.X 球后恢复`
- **解读**：数值越低越好——说明球员被连续得分后能迅速"刹车"

#### 2h. 势头中断效率 (3+连失恢复)
连失 3 分以上的恢复能力（类似 2g，但针对更严重的失分）。

- **计算**：同 2g，但只统计 `lossSeq ≥ 3` 的序列
- **输出**：`mc3Sum / mc3Total`
- **H2H 中展示为**：① 势头中断效率（3+连失后恢复球数）

---

### 维度 3: 关键分与心理边界

将比赛分为**常规分阶段**和**关键分阶段**，对比球员在两个阶段的表现差异。

#### 3a. 关键分定义
- **关键分**：单局中**双方比分 ≥20** 时的每一分（含超分阶段）
- **常规分**：双方比分 <20 时的每一分

#### 3b. 关键分得分率
关键分阶段的得分占比。

- **计算**：
  1. 遍历 `pts`，检测到 `p[0] ≥ 20 || p[1] ≥ 20` 时
  2. 计算这一分是否是本方获得（`sg > 0`）
  3. `clutchWon / clutchPoints * 100`
- **展示**：百分比

#### 3c. 得分自相关 (Lag-1 Autocorrelation)
得分序列的"模式"——波动型 vs 稳态型。

- **计算**：
  1. 将 `pts` 序列转化为信号序列 `sig = [1, -1, 1, ...]`（得 1 分，失 1 分）
  2. 计算 Lag-1 皮尔逊相关系数：`Σ(sig[i]-μ)(sig[i-1]-μ) / √(Σ(sig[i]-μ)²·Σ(sig[i-1]-μ)²)`
  3. 对所有局的值取平均
- **输出**：`r`（无量纲）
- **解读**：
  - `r > 0.15`：连续得分倾向（**波动型**——"神经刀"，能一波带走也能连续丢分）
  - `r < -0.1`：交替得分倾向（**反转型**——有来有回）
  - `-0.1 < r < 0.15`：稳态型（无明显倾向）

#### 3d. 连失分后胜率衰减 (Collapse Threshold)
当一局中出现 N+ 连失分时，该局最终获胜的占比。

- **计算**：
  1. 对每局找到最长连失长度 `maxLoss`
  2. 若 `maxLoss ≥ N`，计入 `collNTotal`
  3. 若该局最终获胜，计入 `collNWon`
- **输出**：`coll2Won/Total`, `coll3Won/Total`, `coll4Won/Total`, `coll5Won/Total`
- **展示**：`w/t * 100 %` + `(t 局)`
- **解读**：从 2→3→4→5 连失的胜率下降斜率，反映"崩盘阈值"

#### 3e. 恢复效率 — 丢分后恢复球数 (Recovery)
**关键分阶段** vs **常规分阶段**：丢分后平均需要多少球才能重新得分。

- **计算**：
  1. 遍历 `pts`，每当球员**丢分**（`sc ≤ 0`且非局末点），记录"到下一次得分之间丢了多少分"
  2. 检查丢分时比分是否为关键分阶段（`p[0] ≥ 18 || p[1] ≥ 18`）
  3. 分别对关键分和常规分求和
- **输出**：
  - `clutchLossDistance / clutchLossN` → 关键分失后恢复球数
  - `normalLossDistance / normalLossN` → 常规分失后恢复球数
- **Δ演变** = `(关键分恢复 - 生涯关键分恢复基准) - (常规分恢复 - 生涯常规分恢复基准)`
- **解读**：双方都 ≥18 分时的丢分恢复效率与常规阶段的差值。正值 = 关键分时恢复变慢，负值 = 关键分时反而恢复更快

#### 3f. 迟滞效率 — 丢分后间隔时间 (Delay)
**关键分阶段** vs **常规分阶段**：丢分后下一分数的间隔时间。

- **计算**：
  1. 使用 `dt`（点间间隔）数据
  2. 每当球员**丢分**，记录该丢分点到下一分之间的时间间隔 `setDt[i]`
  3. 检查丢分时比分是否为关键分阶段
- **输出**：
  - `clutchLossDtSum / clutchLossDtN` → 关键分失后迟滞（秒）
  - `normalLossDtSum / normalLossDtN` → 常规分失后迟滞（秒）
- **Δ演变**：同 3e，但基于秒数
- **解读**：丢分后间隔越长→可能表示犹豫/紧张/心理波动。关键分 vs 常规分的差值衡量"压力下的节奏变化"

#### 3g. 局末学习曲线 (Segmented Scoring Rate)
将一局分为三段，分别计算各段的得分率。

- **计算**：
  1. 1-11 分区间：`seg1W / seg1Pt * 100`
  2. 12-18 分区间：`seg2W / seg2Pt * 100`
  3. 19-21+ 分区间（局末）：`seg3W / seg3Pt * 100`
- **展示**：3 个百分比 + 斜率
- **斜率** = `seg3得分率 - seg1得分率`
  - **正斜率**：越到局末打得越好（学习型球员）
  - **负斜率**：开局强势但局末走弱（体能或专注力问题）

---

### 维度 4: 战术弹性

测度球员在 11 分间歇前后、局间（G1→G2→G3）的调整适应能力。

#### 4a. 间歇前后得分率 (ΔA)
11 分间歇前的得分率 vs 间歇后**前 5 分**的得分率之差。

- **计算**：
  1. 在每局 `pts` 中找到"某方第一次达到 11 分"的位置 `breakIdx`
  2. 全局（所有局汇总）：
     - 间歇前：截至 `breakIdx` 的每一分——`adaptPreAllW / adaptPreAllPt * 100`
     - 间歇后：`breakIdx+1` 到 `breakIdx+5` 的每一分——`adaptPost5W / adaptPost5Pt * 100`
  3. ΔA = 间歇后得分率 − 间歇前得分率
- **展示**：3 个数字（间歇前%、回后5分%、ΔA%）
- **解读**：
  - ΔA > 0：间歇后表现更好 → 战术调整有效
  - ΔA < 0：间歇后表现更差 → 被对手中场针对

#### 4b. 局间适应 (G1→G2 进化)
逐局的 ΔA 指标，追踪从 Set 1 到 Set 3 的调整轨迹。

- **计算**：同上，但按局分别统计：
  - `set1PreAllW/set1PreAllPt` 和 `set1Post5W/set1Post5Pt` → 第 1 局 ΔA
  - `set2PreAllW/set2PreAllPt` 和 `set2Post5W/set2Post5Pt` → 第 2 局 ΔA
  - `set3PreAllW/set3PreAllPt` 和 `set3Post5W/set3Post5Pt` → 第 3 局 ΔA
- **进化** = **G2ΔA − G1ΔA**：从第一局到第二局的调整能力
  - 正数 = 学习进化（被研究后能反调整）

---

### 维度 5: 体能与节奏

测度球员在高强度对抗中的体能维持和节奏控制。

#### 5a. 时长胜率曲线 (Stamina Curve)
按比赛时长分组，展示各时长区间的胜率。

- **计算**：将比赛按 `dur` 分组：
  - 0-50 分钟、50-60、60-70、70-80、80-90、90 分钟+
  - 每组内统计 `w / t * 100%`
- **展示**：折线图（X 轴 = 时长分组，Y 轴 = 胜率）

#### 5b. 消耗战得分率 (Rally Stamina)
高强度回合（>25s）后下一分的得分率。

- **计算**：
  1. 遍历 `dt`，找到 `dt[i] > 25` 的回合（即前一回合耗时 > 25s）
  2. 检查该回合后的下一分是否得分
  3. `rallyStaminaWon / rallyStaminaTotal * 100`
- **展示**：百分比

#### 5c. 体能衰减 (Late-Game Penalty)
60 分钟以上比赛与所有比赛在压制力指标上的衰减。

- **计算**：
  - 常规 3+连得频率（所有局）：`streak3pFreq / totalSets`
  - ≥60分钟比赛 3+连得频率：`lateStreak3pFreq / lateSets`
  - 衰减 = `((常规频率 - 晚期频率) / 常规频率) * 100`
- **展示**：常规连3+ → 晚期连3+ → 衰减百分比

#### 5d. 休整节奏 (Tactical Rest)
消耗战回合后，球员下一间隔的准备时间。

- **计算**：
  1. 遍历 `dt`，找到 `dt[i-1] > 25` 的回合
  2. 记录该回合本身的时间 `dt[i]`（即打完消耗战后球员花了多少秒准备下一分）
  3. `tacticalRestSum / tacticalRestN`
- **展示**：秒
- **解读**：数值越短→越急于发动快攻，不给对手恢复时间

#### 5e. 抗投降指数 (Anti-Surrender)
局末（最后 5 分内）落后 ≥5 分时的丢分间隔。

- **计算**：
  1. 对每局最后 6 分（倒数第 6 个 event），检查分差 `d ≤ -5`
  2. 若该条件满足，记录该丢分的间隔时间
  3. `antiSurrenderDtSum / antiSurrenderDtN`
- **展示**：秒
- **解读**：间隔越长→即使大比分落后也不放弃，积极周旋

#### 5f. Δt 节奏分布 (Δx Distribution)
所有点的点间间隔分布。

- **数据源**：`dt` 数组
- **展示**：散点图（Y 轴 = Δt 秒数，X 轴 = 时间顺序）或直方图
- **关键分 vs 常规分对比**：同 3f

---

## 二、H2H 博弈指标（7 维）

当两名球员被选中进行 H2H 分析时，系统计算 7 维对比指标：

| # | 指标名 | 公式/数据源 | 说明 |
|---|--------|------------|------|
| ① | **节奏克制率** | 消耗战得分率 H2H 值对比（同 5b，但限于两人 H2H 样本） | 在 >25s 高强度对抗中谁的续攻能力更强 |
| ② | **势头中断效率** | mc3 H2H 值对比（同 2h） | 被连得 3 分以上后谁断对方节奏更快 |
| ③ | **局末惯性 Δr** | H2H 自相关 r − 生涯自相关 r（同 3c） | H2H 环境中谁被对方带偏节奏，正值=被拖入波动 |
| ④ | **关键分抢夺差** | H2H关键分得分率 − 生涯关键分得分率（同 3b） | 面对特定对手时关键分表现是升还是降 |
| ⑤ | **战术响应延迟** | 消耗战后下一分的间隔（同 5d），H2H 值 | H2H 环境下谁能更快重新组织进攻 |
| ⑥ | **恢复效率(关键vs常规Δ)** | 3e 的双矩阵对比（球数） | 关键分时丢分恢复 vs 常规时丢分恢复，再对比生涯基准 |
| ⑦ | **迟滞效率(关键vs常规Δ)** | 3f 的双矩阵对比（秒数） | 同 ⑥，但基于时间间隔 |

⑥ 和 ⑦ 展示为 **2×3 矩阵**（每球员一个）：
```
        H2H值    H2H-生涯   生涯值
常规分    X        +Δ         Y
关键分    X'       +Δ'        Y'
        Δ演变 = (关键Δ) - (常规Δ)
```

---

## 三、核心展示公式速查

| 参数 | 公式 | 单位 |
|------|------|------|
| 胜率 | `matchWon / matchTotal * 100` | % |
| 局胜率 | `setWon / totalSets * 100` | % |
| 第三局胜率 | `thirdSetWon / thirdSet * 100` | % |
| 翻盘率(落后4分) | `cb[4].w / cb[4].d * 100` | % |
| 领先3分被翻 | `lead3SetLoss / lead3SetTotal * 100` | % |
| 连3+频率 | `streak3pFreq / totalSets` | 次/局 |
| 连3+出现率 | `streak3pSets / totalSets * 100` | % |
| 止损效率 | `mcSum / mcTotal` | 球 |
| 势头中断效率 | `mc3Sum / mc3Total` | 球 |
| 关键分得分率 | `clutchWon / clutchPoints * 100` | % |
| 自相关 r | `lagAutoSum / lagAutoN` | 无量纲 |
| 连失N分后胜率 | `collNWon / collNTotal * 100` | % |
| ΔA | `(post5/post5Pt - preAll/preAllPt) * 100` | % |
| 进化(G2-G1) | `ΔA_G2 - ΔA_G1` | % |
| 消耗战得分率 | `rallyStaminaWon / rallyStaminaTotal * 100` | % |
| 休整节奏 | `tacticalRestSum / tacticalRestN` | s |
| 抗投降指数 | `antiSurrenderDtSum / antiSurrenderDtN` | s |
| 恢复效率(关键分) | `clutchLossDistance / clutchLossN` | 球 |
| 恢复效率(常规分) | `normalLossDistance / normalLossN` | 球 |
| 迟滞(关键分) | `clutchLossDtSum / clutchLossDtN` | s |
| 迟滞(常规分) | `normalLossDtSum / normalLossDtN` | s |

---

## 四、样本量可靠性评分

系统为每个参数附加一个可靠性得分：

```
rel(n, threshold) = min(1.0, n/threshold)
```

| 参数 | 阈值 n | 标签 |
|------|--------|------|
| 比赛(5卡标题) | 20 | 比赛 |
| 局(5卡标题) | 100 | 局 |
| 翻盘率(落后4分) | 30 | 落后4分局 |
| 翻盘率(落后5分) | 25 | 落后5分局 |
| 关键分 | 50 | 关键分 |
| 连失2分 | 40 | 连失2分局 |
| 连失3分 | 30 | 连失3分局 |
| 止损效率 | 30 | 连失序列 |
| 势头中断效率 | 20 | 3+连失序列 |
| 消耗战回合 | 30 | 消耗战回合 |
| 消耗战后 | 30 | 消耗战后 |
| 抗投降 | 15 | 末段落后 |
| 1-11分段 | 200 | 1-11分 |
| 19-21分段 | 40 | 19-21分 |
| H2H | 10 | 交手场 |

🟢 ≥60% 充足 | 🟡 30-60% 一般 | 🔴 <30% 不足
